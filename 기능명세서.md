# ☕ 칵옹 — 카공 카페 추천 서비스 기능명세서

> v1.0 MVP | 2026년 5월

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 서비스명 | 칵옹 (카공 카페 추천 서비스) |
| 버전 | v1.0 MVP |
| 작성일 | 2026년 5월 |
| 타겟 사용자 | 카페에서 공부/작업하는 사람 (1차: 본인, 2차: 지인) |
| 플랫폼 | 웹 (모바일 브라우저 최적화) |
| 상태 | MVP 배포 완료 — kak-ong.vercel.app |

### 1.1 배경 및 문제 정의

- 카공하기 적합한 카페를 찾는 것은 번거롭다.
- 기존 지도 서비스는 콘센트·시간제한·혼잡도 등 카공 특화 정보를 체계적으로 제공하지 않는다.
- 신뢰할 수 있는 지인 기반의 검증된 카공 카페 정보를 지역별로 쉽게 찾을 수 있는 서비스가 필요하다.

### 1.2 목표

- **MVP**: 본인이 직접 사용하며 데이터 검증 및 UX 개선
- **v1.1**: 지인 그룹 확장, 카페 제보 기능 추가
- **v2.0**: 유저 리뷰, 실시간 혼잡도 등 커뮤니티 기능

---

## 2. 기술 스택

| 영역 | 스택 |
|------|------|
| 프레임워크 | Next.js 16 (App Router, Turbopack) |
| 스타일링 | Tailwind CSS v4 |
| 지도 | Kakao Maps JavaScript SDK |
| 데이터 | 목 데이터 (mock JSON, 추후 Supabase 전환 예정) |
| 배포 | Vercel |
| 폰트 | 양구 조굵은 고딕 (next/font/local) |

---

## 3. 화면 흐름

```
인트로 화면
    ↓ 칵옹하러 가기
지역 선택 화면
    ↓ 지역 탭
필터 선택 화면
    ↓ 다 선택했어요
메인 화면 (지도 + 카페 리스트)
    ↓ 카페 카드 탭
카카오맵 앱/웹 (외부 연결)
```

---

## 4. 화면별 기능 명세

### 4.1 인트로 화면

- 서비스 아이덴티티: `=^•ﻌ•^=` / `공부할 카페 찾기 서비스` / `칵옹` (76px)
- CTA 버튼: `칵옹하러 가기` → 지역 선택 화면으로 이동

### 4.2 지역 선택 화면

- 타이틀: `어디로 칵옹하러 갈까요?`
- 선택 가능 지역 (5개):
  - 강남
  - 홍대 · 합정 · 마포
  - 영등포 · 문래
  - 용산 · 이태원 · 한남
  - 성수
- 선택 상태: 흰 배경 + 검정 테두리 / 미선택: `#f4f4f4` 배경
- 뒤로 버튼: `← 뒤로` → 인트로로 이동

### 4.3 필터 선택 화면

- 타이틀: `필터를 선택해주세요`
- 필터 항목 (7개, 복수 선택 가능):

| 필터명 | 매핑 필드 |
|--------|-----------|
| 콘센트가 있으면 좋겠어요 | `outlet != 'none'` |
| 제한시간이 없었으면 좋겠어요 | `time_limit == 'none'` |
| 밝은곳이 좋아요 | `bright == true` |
| 와이파이가 있어요 | `wifi == true` |
| 조용한 분위기가 좋아요 | `noise_level == 'quiet'` |
| 큰 테이블이 있으면 좋겠어요 | `large_table == true` |
| 1인 좌석이 있으면 좋겠어요 | `single_seat == true` |

- 선택된 항목: AND 조건으로 카페 필터링
- 필터 미선택 시 해당 지역 전체 카페 표시
- CTA: `다 선택했어요` → 메인 화면으로 이동

### 4.4 메인 화면

**레이아웃 (위→아래)**
1. 헤더: `← 다시 찾기` 버튼 + `{지역}에서 칵옹하기` 타이틀
2. 카카오 지도 (카페 마커 표시, 터치/확대 가능)
3. 하단 카드: 필터 태그 가로 스크롤 + 카페 리스트

**필터 태그 바**
- 7개 전체 필터를 가로 스크롤로 표시
- 선택된 필터: 검정 배경 + 흰 텍스트
- 미선택 필터: `#f4f4f4` 배경 + `#a3a3a3` 텍스트

**카페 카드**
- 100×100 회색 썸네일 (추후 실제 이미지로 대체 예정)
- 카페명 (24px), 주소 (14px), 메모 (있을 경우)
- 카드 전체 탭 → 카카오맵으로 이동 (`map.kakao.com/link/map/{name},{lat},{lng}`)

**카카오 지도**
- 마커 탭 → 인포윈도우에 카페명 표시

---

## 5. 데이터 모델

### Cafe

| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| `id` | string | ✅ | 고유 식별자 |
| `name` | string | ✅ | 카페명 |
| `address` | string | ✅ | 전체 주소 |
| `region` | string | ✅ | 지역명 (REGIONS 상수 중 하나) |
| `lat` | number | ✅ | 위도 |
| `lng` | number | ✅ | 경도 |
| `outlet` | `'many'⎮'some'⎮'none'` | ✅ | 콘센트 수 |
| `wifi` | boolean | ✅ | 와이파이 여부 |
| `time_limit` | `'none'⎮'2h'⎮'custom'` | ✅ | 시간제한 |
| `noise_level` | `'quiet'⎮'normal'⎮'noisy'` | ✅ | 혼잡도 |
| `bright` | boolean | ✅ | 밝은 분위기 여부 |
| `large_table` | boolean | ✅ | 큰 테이블 여부 |
| `single_seat` | boolean | ✅ | 1인 좌석 여부 |
| `memo` | string | ❌ | 관리자 메모 |
| `hours` | string | ❌ | 영업시간 |
| `kakao_url` | string | ❌ | 카카오 지도 URL (없을 경우 좌표 기반 자동 생성) |
| `naver_url` | string | ❌ | 네이버 지도 URL |
| `status` | `'active'⎮'pending'⎮'inactive'` | ✅ | 노출 상태 |
| `created_at` | string | ✅ | 등록일시 (ISO 8601) |

---

## 6. 현재 등록된 카페 (목 데이터)

| 카페명 | 지역 |
|--------|------|
| 리브레리 | 영등포 · 문래 |
| 어니언 성수 | 성수 |
| 커피한약방 | 홍대 · 합정 · 마포 |
| 카페 도토리 | 강남 |
| 블루보틀 한남 | 용산 · 이태원 · 한남 |
| 문래 카페 언노운 | 영등포 · 문래 |

---

## 7. 기능 구현 현황

| ID | 기능명 | 상태 |
|----|--------|------|
| F-01 | 카페 목록 조회 | ✅ 완료 |
| F-02 | 지역별 필터 | ✅ 완료 |
| F-03 | 카공 조건 필터 (7가지) | ✅ 완료 |
| F-04 | 카카오 지도 + 마커 | ✅ 완료 |
| F-05 | 카페 탭 → 카카오맵 연결 | ✅ 완료 |
| F-06 | 카페 등록 (Admin) | ⬜ 미구현 |
| F-07 | 카페 수정/삭제 (Admin) | ⬜ 미구현 |
| F-08 | 카페 제보 (유저) | ⬜ 미구현 |
| F-09 | 즐겨찾기 | ⬜ 미구현 |
| F-10 | 실제 카페 이미지 | ⬜ 미구현 |
| F-11 | DB 연동 (Supabase) | ⬜ 미구현 |

---

## 8. 개발 로드맵

| 단계 | 버전 | 주요 기능 |
|------|------|-----------|
| Phase 1 | **v1.0 MVP** ✅ | 마법사 플로우, 지역·조건 필터, 카카오 지도, 카카오맵 연결 |
| Phase 2 | v1.1 | Supabase DB 연동, Admin 카페 등록/수정/삭제, 실제 이미지 |
| Phase 3 | v1.2 | 카페 제보 기능, 즐겨찾기 (로컬스토리지) |
| Phase 4 | v2.0 | 유저 리뷰·평점, 공유 링크, 회원가입/로그인 |
